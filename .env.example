# API 配置模板
# 复制此文件为 .env 并填入实际值

# API 密钥
# 获取方式： https://panda.baidu-int.com/setting
API_KEY=<your-api>

# API 基础 URL
BASE_URL=http://llms-se.baidu-int.com:8200

# 模型名称（可选，默认为 gpt-4o-mini）
MODEL_NAME=gpt-4o-mini

# 代码审查仓库目录（可选，默认 Agent 所在路径）
CR_REPO_PATH=<path-to-your-repo>

# 代码审查 domain 白名单（可选，逗号分隔，留空表示全部启用）
# 示例：CR_AGENT_DOMAIN_WHITELIST=SEC,PERF
CR_AGENT_DOMAIN_WHITELIST=

# agent流程可视化
LANGSMITH_TRACING=true
LANGSMITH_PROJECT=cr-agent
LANGSMITH_API_KEY=<your-lang-smith-api-key>

# MCP配置
CONTEXT7_API_KEY=<your-context7-api-key>

# Git 差异配置
# 设置 Git diff 显示的上下文行数（默认 3 行）
CONTEXT_LINES=3
