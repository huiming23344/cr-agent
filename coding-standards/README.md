# Coding Standards

该目录用于集中管理代码规范，包括规范注册表、运行 profile 以及具体规则描述。结构如下：

- `registry.yaml`：记录当前支持的规则集合，以及各语言、Profile 的映射。
- `rules/<lang>/`：具体规则详情文档，按语言组织。

添加新规则时，建议同步更新 `registry.yaml` 并在相应语言目录下创建 Markdown 说明文件。
